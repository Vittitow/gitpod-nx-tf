{
  "executors": {
    "terraform-apply": {
      "implementation": "./terraform-apply/terraform-apply.impl",
      "schema": "./terraform-apply/schema.json",
      "description": "Runs apply tasks for terraform app-envs."
    },
    "terraform-build": {
      "implementation": "./terraform-build/terraform-build.impl",
      "schema": "./terraform-build/schema.json",
      "description": "Runs build tasks for terraform apps, app-envs and libs."
    },
    "terraform-lint": {
      "implementation": "./terraform-lint/terraform-lint.impl",
      "schema": "./terraform-lint/schema.json",
      "description": "Runs lint tasks for terraform apps and libs."
    },
    "terraform-plan": {
      "implementation": "./terraform-plan/terraform-plan.impl",
      "schema": "./terraform-plan/schema.json",
      "description": "Runs plan tasks for terraform app-envs and libs."
    }
  }
}
