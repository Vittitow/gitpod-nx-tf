{
  "$schema": "http://json-schema.org/schema",
  "type": "object",
  "cli": "nx",
  "properties": {
    "env": {
      "description": "Name of the environment to build.",
      "type": "string"
    }
  }
}