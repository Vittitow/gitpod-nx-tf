{
  "$schema": "http://json-schema.org/schema",
  "cli": "nx",
  "$id": "terraform-app-env",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "Application environment name",
      "$default": {
        "$source": "argv",
        "index": 0
      }
    }
  },
  "required": ["name"]
}
