{
  "$schema": "http://json-schema.org/schema",
  "cli": "nx",
  "$id": "tf-lib",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "Library name",
      "$default": {
        "$source": "argv",
        "index": 0
      },
      "x-prompt": "What's that name of the new terraform library?"
    }
  },
  "required": ["name"]
}
